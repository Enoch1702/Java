{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "jdk",
            "request": "launch",
            "name": "Run Active Java File",
            "mainClass": "${file}",
            "cwd": "${fileDirname}"
        }
    ]
}